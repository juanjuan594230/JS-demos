数组相关

1. for 和 forEach 区别？
  forEach 没法 break
  forEach 用到了函数，所以每次迭代都会有一个新的函数作用域；而 for 循环只有一个作用域
2. 实现数组的迭代方法，map和filter可以有reduce方法表示
3. Underscore.js 集合操作的库
4. split('_') string -> array  join('_')  array -> string